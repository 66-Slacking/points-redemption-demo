# 项目简要说明

## 已完成范围

这是一个本地运行的积分兑换 Web App。用户可注册、登录，领取三个预置任务的积分，查看按用户隔离的余额与收支记录，并用积分兑换 `ebook`、`course`、`theme` 三种数字商品。页面显示兑换订单的处理中、成功或失败状态；成功时显示兑换码。项目使用 TypeScript、Express 和 PostgreSQL，不涉及真实支付或管理后台。

## 关键设计与取舍

- 积分以 `ledger` 流水为准。领分、下单扣分、确定未发码后的退分都使用数据库事务；同一用户的积分操作先锁定用户行，防止并发请求花超余额。领取记录、订单和流水另有唯一约束。
- 一笔兑换始终使用同一个 `requestId`。服务端先按该编号查询外部结果，明确未找到后再请求发码；重试也沿用原编号。外部 API 文档保证同一 Key、同一编号和商品的重复请求返回原结果。
- 网络超时、限流或服务错误时保留“处理中”和已扣积分，供用户稍后查询原订单；收到明确的 `422 PRODUCT_UNAVAILABLE` 才标记失败并退分。页面由服务端生成普通 HTML，减少演示项目的代码量。

## 验证方式与已知限制

按 [README](README.md) 初始化主库和独立测试库，运行 `npm test`。自动化测试使用本地模拟发码服务，覆盖重复领分、重复下单、并发超额兑换、跨用户读取订单、发码后回复丢失的恢复，以及商品不可用时只退一次分。注册、两用户积分隔离、退出登录和未配置 Key 时不扣分也可在页面手动检查。

题目提供的真实 API Key 未包含在仓库中，因此尚未对真实服务执行发码调用；获得有效 Key 后仍需做一次少量真实调用的验收。外部结果不明时由用户在订单详情页手动查询并重试。若 Key 更换时旧订单仍未确认，需要人工联系发码方核对，程序不会用新 Key 盲目重发。
