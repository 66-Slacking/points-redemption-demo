{
  "name": "points-redemption-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm run build && node dist/scripts/setup.js",
    "dev": "npm run build && node dist/src/server.js",
    "build": "tsc",
    "start": "node dist/src/server.js",
    "test": "npm run build && node --test dist/tests/flow.test.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "@types/pg": "^8.15.5",
    "typescript": "^5.9.2"
  }
}
